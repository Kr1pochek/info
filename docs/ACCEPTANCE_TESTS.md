# Приёмочные проверки

## Автоматическая проверка

Из корня проекта:

```powershell
npm run verify
```

Команда выполняет lint, production-сборку, API-тесты и Chromium-проверку интерфейса. Responsive-тест охватывает `/kiosk`, `/packages`, пакет «Прогресс», обе информационные карточки, `/news` и `/admin/login` в четырёх режимах:

- Full HD 1920×1080, 100%;
- вертикальный Full HD 1080×1920, 100%;
- QHD 2048×1152, 125%;
- 4K 2560×1440, 150%.

Тест проверяет HTTP 200, отсутствие ошибок JavaScript, горизонтального переполнения, активные CSS-анимации прогресс-бара и бегущей строки, отсутствие кнопки режима зрения в новостной ленте, локализованную ошибку входа, успешную авторизацию, сохранение казахского языка в защищённых разделах и наличие всех настроек озвучивания. API-тест дополнительно изменяет и восстанавливает параметры озвучивания, подтверждая полный цикл сохранения.

## Ручная матрица

| Область | Проверка | Результат |
| --- | --- | --- |
| Каталог | 42 услуги официального реестра КГД, поиск на двух языках | Все двуязычные карточки открываются без внешних переходов |
| Пакеты | 6 пакетов; «Прогресс» содержит 42 услуги | Данные и зоны отображаются на двух языках |
| Новости | Казахская и русская фазы | После второй фазы включается следующий слайд |
| Эфир | Верхний progress и нижний ticker | Обе анимации движутся непрерывно |
| Сенсор | Клик вне кнопки новостей | Эфир не закрывается |
| Роли | Вход как `EDITOR` | Видны только «Новости» и «Эфир» |
| Ошибка сети | Отключение API после первого показа | Используется сохранённый публичный контент |
| Production | Перезапуск Windows | Сервер и kiosk-браузер запускаются автоматически |
| Backup | Запуск `backup.ps1` | Созданы `.dump` и архив uploads |
| Restore | Запуск `restore.ps1` на отдельной тестовой базе | Дамп и uploads восстановлены, health-check и полный verify успешны |
| Самовосстановление | Принудительное завершение процесса Node.js | Планировщик автоматически поднимает backend, kiosk снова получает ready-ответ |
| Озвучка СЭО | Три языка, префиксы `эй/би/си/ди`, составные номера 1–1000 | Префикс, число и реплика воспроизводятся одним голосом без заметного разрыва |
| Доступность | Нажатие кнопки с пиктограммой глаза | Включаются увеличенный шрифт и высококонтрастная тема |
| Административный язык | Переключение `Рус/Қаз` на входе и в панели | Навигация, все рабочие формы, превью, статусы, аудит и типовые ошибки API меняют язык без перезагрузки |
| Настройки озвучивания | Изменение языка, громкости и интервала повторов | Значения проходят проверку, сохраняются централизованно и доступны публичному контуру табло |
| Инфокиоск | Дата/время, права налогоплательщика, уполномоченный по этике, сроки | Данные постоянно видны или открываются из главного экрана и переключаются RU/KZ |
| ТВ‑панель | Дата/время, специалисты и QR‑коды | Дата/время постоянны; показываются только активные специалисты на сегодня и загруженные QR |

## Что требует стенда заказчика

Следующие пункты нельзя принять только на локальном компьютере: NOMAD WebSocket/API, печать талона, сканер/принтер, повышенная громкость табло, специальный статус талона и уведомление специалиста, операторский native-клиент и сценарии реальной электронной очереди. Физическое подключение ТВ‑1/ТВ‑2 исключено из нашей зоны ответственности.
