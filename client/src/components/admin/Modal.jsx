import { X } from 'lucide-react';
export function Modal({ title, children, onClose, wide = false }) { return <div className="admin-modal-backdrop"><section className={`admin-modal ${wide ? 'admin-modal--wide' : ''}`} role="dialog" aria-modal="true" aria-labelledby="admin-modal-title"><header><h2 id="admin-modal-title">{title}</h2><button onClick={onClose} aria-label="Закрыть"><X /></button></header><div className="admin-modal__body">{children}</div></section></div>; }
export function ConfirmDialog({ title, text, onConfirm, onCancel, busy }) { return <Modal title={title} onClose={onCancel}><p className="confirm-text">{text}</p><div className="form-actions"><button className="admin-button admin-button--secondary" onClick={onCancel}>Отмена</button><button className="admin-button admin-button--danger" onClick={onConfirm} disabled={busy}>{busy ? 'Удаление…' : 'Удалить'}</button></div></Modal>; }
