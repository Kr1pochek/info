import { X } from 'lucide-react';
import { useAdminI18n } from '../../utils/adminLocalization.js';
export function Modal({ title, titleKz, children, onClose, wide = false }) { const { tr } = useAdminI18n(); return <div className="admin-modal-backdrop"><section className={`admin-modal ${wide ? 'admin-modal--wide' : ''}`} role="dialog" aria-modal="true" aria-labelledby="admin-modal-title"><header><h2 id="admin-modal-title">{tr(title, titleKz)}</h2><button onClick={onClose} aria-label={tr('Закрыть')}><X /></button></header><div className="admin-modal__body">{children}</div></section></div>; }
export function ConfirmDialog({ title, titleKz, text, textKz, onConfirm, onCancel, busy }) { const { tr } = useAdminI18n(); return <Modal title={title} titleKz={titleKz} onClose={onCancel}><p className="confirm-text">{tr(text, textKz)}</p><div className="form-actions"><button className="admin-button admin-button--secondary" onClick={onCancel}>{tr('Отмена')}</button><button className="admin-button admin-button--danger" onClick={onConfirm} disabled={busy}>{busy ? tr('Удаление…') : tr('Удалить')}</button></div></Modal>; }
