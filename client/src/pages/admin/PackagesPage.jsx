import { useCallback, useEffect, useState } from 'react';
import { Edit3, Plus, Search, Trash2 } from 'lucide-react';
import api, { apiMessage } from '../../api/client.js';
import AdminPageHeader from '../../components/admin/AdminPageHeader.jsx';
import Toast from '../../components/admin/Toast.jsx';
import { ConfirmDialog, Modal } from '../../components/admin/Modal.jsx';
import { ErrorState, LoadingState } from '../../components/common/States.jsx';
import AppIcon from '../../components/common/AppIcon.jsx';
import { useAuth } from '../../context/AuthContext.jsx';

const blank = { slug: '', titleRu: '', titleKz: '', targetAudienceRu: '', targetAudienceKz: '', descriptionRu: '', descriptionKz: '', serviceZoneRu: '', serviceZoneKz: '', noteRu: '', noteKz: '', icon: 'Package', isPublished: true, sortOrder: 0, serviceIds: [] };
const icons = ['Package', 'Rocket', 'BriefcaseBusiness', 'TrendingUp', 'Accessibility', 'Baby', 'Award'];
const toForm = (item) => ({ ...item, noteRu: item.noteRu || '', noteKz: item.noteKz || '', serviceIds: item.services?.map((service) => service.id) || [] });

export default function PackagesPage() {
  const { user } = useAuth();
  const [rows, setRows] = useState(null); const [services, setServices] = useState([]); const [search, setSearch] = useState('');
  const [error, setError] = useState(''); const [editing, setEditing] = useState(null); const [deleting, setDeleting] = useState(null); const [busy, setBusy] = useState(false); const [toast, setToast] = useState(null);
  const canManage = ['SUPER_ADMIN', 'ADMIN'].includes(user.role);
  const load = useCallback(async () => { setError(''); try { const [packagesResponse, servicesResponse] = await Promise.all([api.get('/admin/service-packages', { params: { search, limit: 100, sort: 'sortOrder', direction: 'asc' } }), api.get('/admin/services', { params: { limit: 100, sort: 'sortOrder', direction: 'asc' } })]); setRows(packagesResponse.data.data); setServices(servicesResponse.data.data); } catch (err) { setError(apiMessage(err)); } }, [search]);
  useEffect(() => { const timer = setTimeout(load, 250); return () => clearTimeout(timer); }, [load]);
  const toggleService = (id) => setEditing((current) => ({ ...current, serviceIds: current.serviceIds.includes(id) ? current.serviceIds.filter((item) => item !== id) : [...current.serviceIds, id] }));
  const save = async (event) => { event.preventDefault(); setBusy(true); setError(''); try { const payload = { ...editing, noteRu: editing.noteRu || null, noteKz: editing.noteKz || null }; if (editing.id) await api.patch(`/admin/service-packages/${editing.id}`, payload); else await api.post('/admin/service-packages', payload); setEditing(null); setToast({ type: 'success', message: editing.id ? 'Пакет обновлён' : 'Пакет создан' }); await load(); } catch (err) { setError(apiMessage(err)); } finally { setBusy(false); } };
  const remove = async () => { setBusy(true); try { await api.delete(`/admin/service-packages/${deleting.id}`); setDeleting(null); setToast({ type: 'success', message: 'Пакет удалён' }); await load(); } catch (err) { setToast({ type: 'error', message: apiMessage(err) }); setDeleting(null); } finally { setBusy(false); } };
  return <><AdminPageHeader eyebrow="Контент" title="Пакеты обслуживания" description="Маршруты посетителей, целевые категории и зоны обслуживания" actions={canManage && <button className="admin-button admin-button--primary" onClick={() => setEditing({ ...blank })}><Plus size={19} />Новый пакет</button>} />
    <div className="admin-toolbar"><label className="admin-search"><Search /><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Поиск пакета" /></label><span>{rows?.length || 0} записей</span></div>
    {error && !editing && <ErrorState title="Не удалось загрузить пакеты" text={error} onRetry={load} />}{!rows && !error ? <LoadingState /> : rows && <div className="admin-card admin-table-wrap"><table className="admin-table"><thead><tr><th>Порядок</th><th>Пакет</th><th>Целевая категория</th><th>Услуги</th><th>Статус</th><th></th></tr></thead><tbody>{rows.map((item) => <tr key={item.id}><td>{item.sortOrder}</td><td><div className="table-title"><span><AppIcon name={item.icon} size={22} /></span><div><strong>{item.titleRu}</strong><small>{item.titleKz} · {item.slug}</small></div></div></td><td>{item.targetAudienceRu}</td><td>{item._count.services}</td><td><span className={`status-pill ${item.isPublished ? 'status-pill--success' : 'status-pill--muted'}`}>{item.isPublished ? 'Опубликован' : 'Скрыт'}</span></td><td>{canManage && <div className="row-actions"><button onClick={() => setEditing(toForm(item))} aria-label="Редактировать"><Edit3 /></button><button onClick={() => setDeleting(item)} aria-label="Удалить"><Trash2 /></button></div>}</td></tr>)}</tbody></table></div>}
    {editing && <Modal wide title={editing.id ? 'Редактирование пакета' : 'Новый пакет'} onClose={() => setEditing(null)}><form className="admin-form" onSubmit={save}><div className="form-grid"><label><span>Название (русский)</span><input required maxLength={160} value={editing.titleRu} onChange={(event) => setEditing({ ...editing, titleRu: event.target.value })} /></label><label><span>Название (қазақша)</span><input required maxLength={160} value={editing.titleKz} onChange={(event) => setEditing({ ...editing, titleKz: event.target.value })} /></label><label className="form-grid__wide"><span>Slug</span><input required pattern="[a-z0-9]+(?:-[a-z0-9]+)*" maxLength={120} value={editing.slug} onChange={(event) => setEditing({ ...editing, slug: event.target.value.toLowerCase() })} /></label><label><span>Целевая категория (русский)</span><textarea required maxLength={800} value={editing.targetAudienceRu} onChange={(event) => setEditing({ ...editing, targetAudienceRu: event.target.value })} /></label><label><span>Целевая категория (қазақша)</span><textarea required maxLength={800} value={editing.targetAudienceKz} onChange={(event) => setEditing({ ...editing, targetAudienceKz: event.target.value })} /></label><label><span>Описание (русский)</span><textarea required maxLength={5000} value={editing.descriptionRu} onChange={(event) => setEditing({ ...editing, descriptionRu: event.target.value })} /></label><label><span>Описание (қазақша)</span><textarea required maxLength={5000} value={editing.descriptionKz} onChange={(event) => setEditing({ ...editing, descriptionKz: event.target.value })} /></label><label><span>Зона (русский)</span><textarea required maxLength={500} value={editing.serviceZoneRu} onChange={(event) => setEditing({ ...editing, serviceZoneRu: event.target.value })} /></label><label><span>Зона (қазақша)</span><textarea required maxLength={500} value={editing.serviceZoneKz} onChange={(event) => setEditing({ ...editing, serviceZoneKz: event.target.value })} /></label><label><span>Примечание (русский)</span><textarea maxLength={1000} value={editing.noteRu} onChange={(event) => setEditing({ ...editing, noteRu: event.target.value })} /></label><label><span>Примечание (қазақша)</span><textarea maxLength={1000} value={editing.noteKz} onChange={(event) => setEditing({ ...editing, noteKz: event.target.value })} /></label><fieldset className="form-grid__wide"><legend>Иконка</legend><div className="icon-picker">{icons.map((icon) => <button type="button" className={editing.icon === icon ? 'selected' : ''} onClick={() => setEditing({ ...editing, icon })} key={icon}><AppIcon name={icon} /><span>{icon}</span></button>)}</div></fieldset><fieldset className="form-grid__wide"><legend>Услуги пакета: {editing.serviceIds.length}</legend><div className="package-service-picker">{services.map((service) => <label key={service.id}><input type="checkbox" checked={editing.serviceIds.includes(service.id)} onChange={() => toggleService(service.id)} /><span>{service.titleRu}</span></label>)}</div></fieldset><label><span>Порядок</span><input type="number" min="0" max="10000" required value={editing.sortOrder} onChange={(event) => setEditing({ ...editing, sortOrder: Number(event.target.value) })} /></label><label className="toggle-label"><input type="checkbox" checked={editing.isPublished} onChange={(event) => setEditing({ ...editing, isPublished: event.target.checked })} /><span>Опубликован</span></label></div>{error && <div className="form-error">{error}</div>}<div className="form-actions"><button type="button" className="admin-button admin-button--secondary" onClick={() => setEditing(null)}>Отмена</button><button className="admin-button admin-button--primary" disabled={busy}>{busy ? 'Сохранение…' : 'Сохранить'}</button></div></form></Modal>}
    {deleting && <ConfirmDialog title="Удалить пакет?" text={`Пакет «${deleting.titleRu}» будет удалён. Услуги останутся в каталоге.`} onConfirm={remove} onCancel={() => setDeleting(null)} busy={busy} />}<Toast {...toast} onClose={() => setToast(null)} />
  </>;
}
